# target-app

This folder represents the application repository that should be scanned by the security platform.

In real usage, this should live as an independent Git repository.
PR and push events from this app should trigger the security platform webhook.
